name: data
description: Data layer for the counter app.
#version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: