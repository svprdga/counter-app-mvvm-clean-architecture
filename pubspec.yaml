name: counter_app_clean_architecture
description: Counter app with MVVM and Clean Architecture.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  domain:
    path: layers/domain
  data:
    path: layers/data
  presentation:
    path: layers/presentation
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^5.0.0

flutter:
  uses-material-design: true